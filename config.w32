// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("jsonrpc", "for jsonrpc support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("jsonrpc", "enable jsonrpc support", "no");

if (PHP_JSONRPC != "no") {
	EXTENSION("jsonrpc", "jsonrpc.c");
}

